import { component$ } from "@builder.io/qwik";

export const Accouncment = component$(() => {
    return (
        <div class="flex w-full rounded-xl bg-neutral-600 p-4 text-white">
            Selfmail announces a $100 Millionen seed round.
        </div>
    );
});
