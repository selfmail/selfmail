# @selfmail/authentication

Framework-agnostic authentication helpers for Selfmail apps.

It exposes an `Authentication` class that works with injected cookie stores,
request metadata, and a session repository so app code can stay thin and
framework-specific adapters can live close to each app.
