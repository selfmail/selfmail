{
	"name": "database",
	"module": "src/index.ts",
	"type": "module",
	"scripts": {
		"db:push": "infisical run --env=dev -- prisma db push",
		"db:push-reset": "infisical run --env=dev -- prisma db push --force-reset",
		"generate": "prisma generate"
	},
	"exports": {
		".": {
			"import": "./src/index.ts",
			"types": "./src/index.ts"
		}
	},
	"devDependencies": {
		"@types/bun": "latest",
		"prisma": "^6.18.0"
	},
	"peerDependencies": {
		"typescript": "^5.8.3"
	},
	"dependencies": {
		"@prisma/client": "^6.18.0",
		"dotenv": "^17.2.3"
	}
}
