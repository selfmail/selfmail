"use client"

/**
 * The default button, configurable.
 * @returns {React.JSX.Element}
 */
export function Button(): React.JSX.Element {
    return (
        <button className="bg-blue-500">
            hello
        </button>
    )
}