# Inbound Stores

E.g. Recipients and connection lists for spam patterns and quick recipient search.

Both packages are build arround a KV Store to quickly access important information for the smtp inboubd service, but both packages don't act as single source of truth 