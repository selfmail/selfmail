# Inbound

**Testing**

We run automatic tests with corrupt files, files that contain spam, viruses, mails that come from a local SMTP server and remote SMTP servers. For that, we are using [testmail](https://testmail.app).